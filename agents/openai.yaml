interface:
  display_name: "灵感火花"
  short_description: "从资料中生成可追溯、可验证、可交接的小产品机会候选记录"
  default_prompt: "Use $idea-spark to turn this source material into source-grounded opportunity records, reject weak candidates, and hand the strongest to business-lens."
policy:
  allow_implicit_invocation: true
